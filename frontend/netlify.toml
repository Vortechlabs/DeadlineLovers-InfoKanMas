[build]
  base = "frontend"
  publish = "frontend/dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"

# Redirect settings untuk React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200